You are analyzing an ad creative for a BNPL (Buy Now Pay Later) fintech app called Tabby, operating in Saudi Arabia and UAE.

Analyze the visual content and return ONLY a valid JSON object with NO markdown formatting, NO backticks, NO explanation. Just raw JSON.

For each field, you MUST select from the allowed values listed below. If no value fits, use your best guess but set confidence to "low".

{
  "topic": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "main_object": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "main_usp": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "object_option_1": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "object_option_2": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "language": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "details": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "sound_type": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "background": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "main_color": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"},
  "hook": {"value": "<SELECT FROM ALLOWED VALUES>", "confidence": "<high|medium|low>"}
}

Rules:
- You MUST select values from the ALLOWED VALUES list below. Do NOT invent new values.
- All values must be lowercase with SPACES between words (NOT underscores). Underscores are field separators.
- Return ONLY JSON, nothing else
- If unsure, set confidence to "low"
- For video: analyze all frames as one creative
- For images: sound_type must be "none"
- Pay attention to Arabic text - detect language accurately
- For main_object: describe what is VISUALLY dominant (people, items, text, etc.) — do NOT say "app ui" unless it is literally a screenshot of the Tabby app interface